// src/components/editor/GridEditor.tsx

export default function GridEditor() {
  // Currently, painting happens via ClickHandler in the 3D scene.
  // This component is a placeholder for future grid overlay UI (e.g., showing
  // a mini-map or cell coordinates).
  return null;
}
